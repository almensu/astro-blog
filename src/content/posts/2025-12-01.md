---
title: 从零开始：用 AI 部署 Hello World 到 Google Cloud Run｜保姆级教程
date: 2025-12-01
summary: 这不是传统的部署教程，而是一个 AI 时代的“思维跃迁”范例。你将看到我如何用一句提示词指挥 AI，从 0 到上线一个完整的 Web 应用到 Google Cloud Run。手动代码部分只占 20%，重点是：如何高效让 AI 为你干活。
category: AI编程实战
tags: [Google Cloud Run, AI编程, 提示词工程, Docker, 部署教程, Cursor, TRAE]
---

这不是传统的部署教程，而是一个 AI 时代的“思维跃迁”范例。

你将看到我如何用一句提示词指挥 AI，从 0 到上线一个完整的 Web 应用。

手动代码部分只占 20%，重点是：如何高效让 AI 为你干活，让普通人也能拥有开发者的能力感。

## 🚀 AI 时代的核心能力：写提示词，不是写代码

AI 编程的关键早已不是记住语法，而是能不能“吩咐 AI 把事办好”。

把提示词写得像一封委托书，AI 才知道你要什么。

无论你用的是 Cursor、TRAE 还是 ChatGPT，下面这段提示词都能瞬间生成完整的部署代码。

这不是炫技，而是一次“交钥匙式”的 AI 提效示范 —— 看完你就会明白：你可以照着做，真的能做成！

## 🔥 提示词示例：部署一个 Cloud Run 服务

将以下提示词发送给任何一个 AI 编程助手：

> 你是一位资深的 Google Cloud 全栈工程师。请帮我从零构建一个可以直接部署到 Google Cloud Run 的 Node.js Web 应用。
>
> 具体要求如下：
>
> 1. 创建一个基于 Express 的 Web 服务器。
> 2. 首页显示 "Hello World"，样式为白底灰字、居中排布。
> 3. 使用 node:18-slim 镜像编写 Dockerfile。
> 4. 一次性输出完整的 package.json, server.js, index.html 和 Dockerfile 四个文件内容。

### 💡 提示词结构拆解（为什么这么写）

| 元素         | 示例                                 | 作用                             |
| ------------ | ------------------------------------ | -------------------------------- |
| 角色设定     | "你是一位资深的 Google Cloud 工程师" | 切换 AI 到专业模式，提高代码质量 |
| 明确目标     | "从零构建可部署的 Node.js 应用"      | 避免生成不相关的样例             |
| 细节约束     | "白底灰字、node:18-slim 镜像"        | 控制生成范围与性能，避免“乱发挥” |
| 交付格式要求 | "一次性输出 4 个完整文件"            | 避免代码分段输出，方便复制粘贴   |

> ❝ 写提示词的质量，决定了你和 AI 合作的效率。❞

## 🧪 AI 输出的四个核心文件（示例）

仅作参考：以下内容均可由提示词一次性生成，无需手写。

你可以跳过这段代码，直接去让 AI 为你生成这些内容。

### 1. package.json

```json
{
  "name": "hello-world-cloudrun",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
```

### 2. server.js

```javascript
const express = require('express')
const path = require('path')
const app = express()
const port = process.env.PORT || 8080
app.get('/', (req, res) => {
  res.sendFile(path.join(__dirname, 'index.html'))
})
app.listen(port, () => {
  console.log(`Server running on port ${port}`)
})
```

### 3. index.html

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Hello World</title>
    <style>
      body {
        background-color: white;
        color: gray;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        font-family: sans-serif;
      }
    </style>
  </head>
  <body>
    <h1>Hello World</h1>
  </body>
</html>
```

### 4. Dockerfile

```dockerfile
FROM node:18-slim
WORKDIR /usr/src/app
COPY package*.json ./
RUN npm install --only=production
COPY . .
CMD [ "node", "server.js" ]
```

## ⚡️ 继续指挥 AI：让它告诉你怎么部署

你不需要记住命令，只需要问 AI：

> 我的代码和 Dockerfile 已经准备就绪。
> 请告诉我如何通过 Google Cloud SDK (gcloud) 将当前目录下的应用部署到 Cloud Run。
>
> 请返回一条完整的 gcloud run deploy 命令，要求：
>
> 1. 服务名为 hello-world-service
> 2. 区域为 us-central1
> 3. 允许公开访问（--allow-unauthenticated）

## 🧠 你只需要做这几件事（手动操作部分）

### 1. 安装 gcloud SDK（Mac 用户）

```bash
brew install --cask google-cloud-sdk
```

### 2. 初始化配置

```bash
gcloud init
```

登录、创建项目、绑定结算账号。

### 3. 一键部署

```bash
gcloud run deploy hello-world-service \
  --source . \
  --region us-central1 \
  --allow-unauthenticated
```

部署成功后你会看到：

```
Service [hello-world-service] ... has been deployed
Service URL: https://hello-world-service-xxxxx.us-central1.run.app
```

点击访问即可！

## ✅ 总结：普通人也能拥有开发者的成就感

如果你看到了这里，恭喜你完成了人生第一次 AI 辅助部署。

这不是一个技术门槛的问题，而是一个“相信你可以做到”的思维门槛。

技术不再是程序员的专属，AI 已经把它变成了每个人的工具。

你需要的，不是更强的手速，而是更清晰的表达。把问题说清楚，AI 就能帮你完成。
